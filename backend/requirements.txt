Flask==2.3.3                    # Flask for building the API
Flask-Cors             # Flask-CORS to handle Cross-Origin Resource Sharing (for React frontend)
transformers==4.30.0            # HuggingFace Transformers for BERT models
torch==2.0.1                    # PyTorch for running BERT model
pandas==2.0.3                   # Pandas for data handling
scikit-learn==1.3.0             # Scikit-learn for cosine similarity (or other ML tasks)
numpy==1.25.2                   # Numpy for numerical operations

